package utils;

public class Enums {
    public enum ZoneType { A, AB, ABC, ABCD, A_GROUP}
    public enum PassDuration { NIGHT, DAY, WEEKEND, THREEDAY, WEEK, MONTH, FOURMONTH }
    public enum PaymentMethod { CASH, CREDIT_CARD, DEBIT_CARD, MOBILE_PAY }
    public enum TVMStatus { ON, OFF, OUT_OF_SERVICE }
    public enum TransactionStatus { SUCCESS, FAILED, REFUNDED }
}

